package com.school.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.school.model.ReferralRole;

public interface ReferralRoleRepo extends JpaRepository<ReferralRole, Integer>{

}
